<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CodeSphere - User Profile</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #4361ee;
            --primary-hover: #3a56d4;
            --text-dark: #1e293b;
            --text-light: #64748b;
            --bg-light: #f8fafc;
            --border-color: #e2e8f0;
        }
        
        body {
            background-color: white;
            color: var(--text-dark);
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
            margin: 0;
        }
        
        .user-container {
            background-color: white;
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 500px;
            border: 1px solid var(--border-color);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            text-align: center;
        }
        
        .user-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
        }
        
        .profile-icon {
            font-size: 3.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }
        
        .user-container h2 {
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: var(--text-dark);
        }
        
        .table {
            margin-top: 1.5rem;
            color: var(--text-dark);
            border-radius: 8px;
            overflow: hidden;
            border: 1px solid var(--border-color);
        }
        
        .table td {
            padding: 12px 15px;
            font-size: 0.95rem;
            border-bottom: 1px solid var(--border-color);
            text-align: left;
        }
        
        .table td:first-child {
            font-weight: 600;
            color: var(--primary-color);
            width: 40%;
        }
        
        .table td:last-child {
            color: var(--text-dark);
        }
        
        .logout-btn {
            background-color: var(--primary-color);
            border: none;
            padding: 12px;
            font-size: 1rem;
            font-weight: 600;
            width: 100%;
            border-radius: 8px;
            transition: all 0.3s ease;
            letter-spacing: 0.5px;
            color: white;
            margin-top: 1.5rem;
            cursor: pointer;
        }
        
        .logout-btn:hover {
            background-color: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(67, 97, 238, 0.2);
        }
        
        .logout-btn:active {
            transform: translateY(0);
        }
        
        @media (max-width: 576px) {
            .user-container {
                padding: 2rem 1.5rem;
            }
            
            .table td {
                padding: 10px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="user-container">
        <i class="fas fa-user-circle profile-icon"></i>
        <h2>CodeSphere User Details</h2>
        <table class="table">
            <tr><td>Name</td><td><%= name %></td></tr>
            <tr><td>Email</td><td><%= email %></td></tr>
            <tr><td>Learning Path</td><td><%= learningPath || "Not specified" %></td></tr>
            <tr><td>Mobile Number</td><td><%= mobile || "Not specified" %></td></tr>
            <tr><td>College Name</td><td><%= college || "Not specified" %></td></tr>
            <tr><td>Current Course</td><td><%= currentCourse || "Not specified" %></td></tr>
            <tr><td>Year Of Graduation</td><td><%= yearOfGraduation || "Not specified" %></td></tr>
            <tr><td>Time Period</td><td><%= timePeriod || "Not specified" %></td></tr> 
        </table>
        <button onclick="window.location.href='/logout'" class="logout-btn">Logout</button>
    </div>
</body>
</html>
